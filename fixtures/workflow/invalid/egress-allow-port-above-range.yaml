# invalid: An egress entry whose port is outside 1 to 65535. The runner proxy
#   opens a connection to the pair, and there is no port above that to open.
# refused by: the schema
# reference: https://agentiik.github.io/docs/#step-keywords

apiVersion: agentiik.dev/v1
kind: Workflow

metadata:
  name: monthly-invoicing
  namespace: finance

steps:
  invoice:
    image: ghcr.io/acme/agk-invoice@sha256:1ab74e66e7966eea770c1042664af5f550650f299ce00e02132ffa4fec5039cc
    network: egress
    egress:
      allow: ["api.billing.example.com:99999"]
    outputs: [out]
