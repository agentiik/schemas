# invalid: A condition reading a secret. secrets is exposed to params and to
#   secrets and nowhere else, and a secret is never reachable from a
#   condition.
# refused by: the validator, beyond what JSON Schema can state
# reference: https://agentiik.github.io/docs/#exposed-context

apiVersion: agentiik.dev/v1
kind: Workflow

metadata:
  name: monthly-invoicing
  namespace: finance

secrets:
  billing:
    provider: vault
    path: kv/data/agentiik/billing

steps:
  invoice:
    image: ghcr.io/acme/agk-invoice@sha256:1ab74e66e7966eea770c1042664af5f550650f299ce00e02132ffa4fec5039cc
    secrets: [billing]
    if: ${{ secrets.billing != "" }}
    outputs: [out]
