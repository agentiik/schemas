# invalid: An include that is both a file of this repository and another
#   repository. A path include resolves inside the same commit and a workflow
#   include resolves elsewhere at a ref; one entry cannot be both.
# refused by: the schema
# reference: https://agentiik.github.io/docs/#includes-and-inheritance

apiVersion: agentiik.dev/v1
kind: Workflow

metadata:
  name: monthly-invoicing
  namespace: finance

include:
  - path: ./common-bricks.yaml
    workflow: finance/common
    ref: v2.1.0

steps:
  normalize:
    image: ghcr.io/acme/agk-normalize@sha256:9f2c1d073f187ad520aaf67af255db9208210cfac76f1f2426ef8d938079b7e0
    outputs: [out]
