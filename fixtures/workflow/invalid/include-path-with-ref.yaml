# invalid: A path include pinned to a ref. A path include resolves inside the
#   same commit, so it can never be stale and there is nothing for a ref to
#   pin; a ref there means as little as a timeout on an async tool.
# refused by: the schema
# reference: https://agentiik.github.io/docs/#includes-and-inheritance

apiVersion: agentiik.dev/v1
kind: Workflow

metadata:
  name: monthly-invoicing
  namespace: finance

include:
  - path: ./common-bricks.yaml
    ref: v2.1.0

steps:
  normalize:
    image: ghcr.io/acme/agk-normalize@sha256:9f2c1d073f187ad520aaf67af255db9208210cfac76f1f2426ef8d938079b7e0
    outputs: [ok]
