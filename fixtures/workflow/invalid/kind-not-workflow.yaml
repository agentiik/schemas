# invalid: A workflow entry point declaring another kind. The same apiVersion
#   covers the brick manifest, so the kind is what tells a reader which of the
#   two it holds.
# refused by: the schema
# reference: https://agentiik.github.io/docs/#yaml

apiVersion: agentiik.dev/v1
kind: Brick

metadata:
  name: monthly-invoicing
  namespace: finance

steps:
  normalize:
    image: ghcr.io/acme/agk-normalize@sha256:9f2c1d073f187ad520aaf67af255db9208210cfac76f1f2426ef8d938079b7e0
    outputs: [out]
