# invalid: A workflow owned by a namespace named runs. The API's first path
#   segment decides the route, so the words it routes on cannot name a namespace.
# refused by: the schema
# reference: https://agentiik.github.io/docs/#scopes-and-resolution

apiVersion: agentiik.dev/v1
kind: Workflow

metadata:
  name: nightly-reconciliation
  namespace: runs

steps:
  reconcile:
    image: ghcr.io/acme/agk-normalize@sha256:9f2c1d073f187ad520aaf67af255db9208210cfac76f1f2426ef8d938079b7e0
    outputs: [out]
