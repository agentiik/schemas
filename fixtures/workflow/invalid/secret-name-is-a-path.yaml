# invalid: A secret named by where it sits in the store. A secret is named on
#   the identifier grammar, the name a step mounts it by and the namespace
#   declares it under, and where its value lives belongs to that declaration
#   rather than to the file.
# refused by: the schema
# reference: https://agentiik.github.io/docs/#secrets

apiVersion: agentiik.dev/v1
kind: Workflow

metadata:
  name: monthly-invoicing
  namespace: finance

secrets: [kv/data/agentiik/billing]

steps:
  invoice:
    image: ghcr.io/acme/agk-invoice@sha256:1ab74e66e7966eea770c1042664af5f550650f299ce00e02132ffa4fec5039cc
    outputs: [out]
