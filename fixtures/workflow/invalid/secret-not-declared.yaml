# invalid: A step mounting a secret the workflow never names. Secrets are
#   mounted by the name the secrets block lists them under, and only secrets
#   the owning namespace declares can be named there.
# refused by: the validator, beyond what JSON Schema can state
# reference: https://agentiik.github.io/docs/#step-keywords

apiVersion: agentiik.dev/v1
kind: Workflow

metadata:
  name: monthly-invoicing
  namespace: finance

steps:
  invoice:
    image: ghcr.io/acme/agk-invoice@sha256:1ab74e66e7966eea770c1042664af5f550650f299ce00e02132ffa4fec5039cc
    secrets: [billing]
    outputs: [out]
