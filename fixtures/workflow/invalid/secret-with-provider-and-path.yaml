# invalid: A secret written with the store it is read from. The namespace
#   declares where each of its secrets lives, provider and path, and is
#   confined to its own paths; the file names a secret and nothing more, so
#   that pushing a workflow cannot aim it at whatever the store holds.
# refused by: the schema
# reference: https://agentiik.github.io/docs/#secrets

apiVersion: agentiik.dev/v1
kind: Workflow

metadata:
  name: monthly-invoicing
  namespace: finance

secrets:
  billing:
    provider: vault
    path: kv/data/agentiik/billing

steps:
  invoice:
    image: ghcr.io/acme/agk-invoice@sha256:1ab74e66e7966eea770c1042664af5f550650f299ce00e02132ffa4fec5039cc
    secrets: [billing]
    outputs: [out]
