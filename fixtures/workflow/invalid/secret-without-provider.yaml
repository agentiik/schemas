# invalid: A secret declared without the store it is read from. The value is
#   never written in the file, so the provider is all there is to resolve it
#   by.
# refused by: the schema
# reference: https://agentiik.github.io/docs/#yaml

apiVersion: agentiik.dev/v1
kind: Workflow

metadata:
  name: monthly-invoicing
  namespace: finance

secrets:
  billing:
    path: kv/data/agentiik/billing

steps:
  invoice:
    image: ghcr.io/acme/agk-invoice@sha256:1ab74e66e7966eea770c1042664af5f550650f299ce00e02132ffa4fec5039cc
    secrets: [billing]
    outputs: [out]
