# Pinned so that the build refuses a document for the same reason on every machine.
# jsonschema brings attrs, referencing and rpds-py with it; of those, only referencing is
# imported here, and so it is pinned beside the two others.
jsonschema==4.25.1
PyYAML==6.0.3
# The registry through which openapi.json's references into wire.schema.json resolve when
# its examples are validated, as a consumer resolves them: jsonschema's own way of holding
# more than one document, with no OpenAPI validator and the opinions it would bring.
referencing==0.36.2
